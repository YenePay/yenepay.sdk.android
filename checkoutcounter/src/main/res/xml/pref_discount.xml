<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->
    <SwitchPreference
        android:defaultValue="false"
        android:key="discount_enable"
        android:title="@string/pref_title_enable_discount_switch"/>
    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_fee_calc_type_titles"
        android:entryValues="@array/pref_fee_calc_type_values"
        android:key="discount_type"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Discount type"
        android:dependency="discount_enable"/>

    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@null"
        android:summary="Enter the discount amount"
        android:inputType="numberDecimal"
        android:key="discount_amount"
        android:maxLines="1"
        android:dependency="discount_enable"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Discount amount" />
    <!-- Allows the user to choose a ringtone in the 'notification' category. -->
    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
    <!--<RingtonePreference-->
        <!--android:defaultValue="content://settings/system/notification_sound"-->
        <!--android:dependency="notifications_new_message"-->
        <!--android:key="notifications_new_message_ringtone"-->
        <!--android:ringtoneType="notification"-->
        <!--android:title="@string/pref_title_ringtone" />-->

    <!--&lt;!&ndash; NOTE: This preference will be enabled only when the checkbox above is checked. &ndash;&gt;-->
    <!--<SwitchPreference-->
        <!--android:defaultValue="true"-->
        <!--android:dependency="notifications_new_message"-->
        <!--android:key="notifications_new_message_vibrate"-->
        <!--android:title="@string/pref_title_vibrate" />-->

</PreferenceScreen>
